# Session switcher, quick idea, and help

Finish remaining overlays: multi-session switch, quick idea, and useful help.

## Prerequisites

- [x] @step load-session-on-scope-bind
- [x] @step ducktui-shell

## Context

Review finding 5 (remainder): session switcher, quick idea, and help after slash/model.
Session switcher builds on latest-session load from step `load-session-on-scope-bind`.

## Tasks

- [x] 1. Session switcher: list sessions for the bound scope; select loads that session
         (displayed); support new session if useful

- [x] 2. Quick idea: capture into the shared idea store path duckboard uses

- [x] 3. Help overlay: real key map (global, panes, overlays) instead of a one-liner

- [x] 4. Close restores pane dispatch; no middle content browser
