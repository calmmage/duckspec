# Placement UI and promotion carry

Minimal Main/Worktree/Auto control on the CHANGE surface, and copy placement when an
exploration promotes into a change.

## Prerequisites

- [x] @step worktree-manager-and-placement-policy

## Tasks

- [x] 1. CHANGE row or change-header control to set placement; default Auto under
         Jj/Worktrees and Main under plain Git

- [x] 2. On exploration→change promotion, copy placement (and worktree binding) onto the
         change scope with the existing remount path

- [x] 3. @spec worktree/scope-placement Placement persists: Promotion keeps placement on the change
