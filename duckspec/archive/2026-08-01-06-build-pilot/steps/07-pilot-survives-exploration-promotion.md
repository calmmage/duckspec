# Pilot survives exploration promotion

Keep armed pilot mode when an exploration session is promoted into a change.

## Prerequisites

- [x] @step kick-and-arm-on-submit

## Tasks

- [x] 1. Assert or fix `promote_exploration` / `merge_sessions` so `pilot` is preserved

- [x] 2. @spec chat/build-pilot Scope continuity: Armed pilot survives exploration promotion to change
