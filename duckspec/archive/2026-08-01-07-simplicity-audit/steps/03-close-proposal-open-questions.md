# Close proposal open questions

Align `proposal.md` with settled design so open questions are no longer listed as
unresolved.

## Context

From review `01-review-simplicity-audit-stock-skill`: hybrid write and judgment-based
explore offer are settled in design and stock templates; proposal still lists them as
open.

## Tasks

- [x] 1. Edit `proposal.md`: record hybrid write (chat-only if no proposal;
         confirm-then-rewrite if present) and judgment-based explore offer (not routine on
         every change)

- [x] 2. Remove `## Open questions` (or replace with settled note / none); `ds format` +
         `ds check` the proposal
